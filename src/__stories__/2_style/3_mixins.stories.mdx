import { Meta } from '@storybook/addon-docs/blocks';
import { Mixins } from './styleComponents'

<Meta title="Style/Mixins" parameters={{previewTabs: {canvas: {hidden: true}}}} />

# Sass Mixins

The WebViewer React Toolkit ships with some Sass mixins that wrap common
patterns. These can only be used in Sass files, but the code is provided below
so that you can implement them in CSS if you wish.

> For more on Sass mixins, check out the
> [Sass documentation](https://sass-lang.com/documentation/at-rules/mixin).

## Using

To use these in your own project, you can include the mixins in your code:

```scss
div {
  @include flex-center;

  @include for-tablet-up {
    background-color: blue;
  }
}
```

Or copy the code into your CSS files:

```css
div {
  display: flex;
  justify-content: center;
  align-items: center;


  @media (min-width: var(--breakpoint-tablet)) {
    background-color: blue;
  }
}
```

## Mixins

The following mixins show the value you can include in your code, as well as the
raw values in case you wish to copy them to css code. You can click any of these
to copy the value to your clipboard.

<!-- Below here is auto-generated. DO NOT EDIT. -->
<!-- GENERATE_ENTRY -->

<Mixins index={0} />

```scss
@mixin focus {
  box-shadow: 0 0 0 2px var(--color-focus-shadow);
}
```


<Mixins index={1} />

```scss
@mixin focus-inset {
  box-shadow: inset 0 0 0 2px var(--color-focus-shadow);
}
```


<Mixins index={2} />

```scss
// Try not to use this, as it breaks "mobile first" principles.
@mixin for-phone-only {
  @media (max-width: var(--breakpoint-tablet) - 1) {
    @content;
  }
}
```


<Mixins index={3} />

```scss
@mixin for-tablet-up {
  @media (min-width: var(--breakpoint-tablet)) {
    @content;
  }
}
```


<Mixins index={4} />

```scss
@mixin for-desktop-up {
  @media (min-width: var(--breakpoint-desktop)) {
    @content;
  }
}
```


<Mixins index={5} />

```scss
@mixin absolute-fill {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
}
```


<Mixins index={6} />

```scss
@mixin fixed-fill {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
}
```


<Mixins index={7} />

```scss
@mixin flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}
```


<Mixins index={8} />

```scss
// This provides the animation for skeleton loading.
@mixin skeleton {
  animation: timeline 2s linear infinite;
  background: linear-gradient(to right, #eee 0%, #ddd 10%, #eee 20%);
  background-size: 600px auto;
}

@keyframes timeline {
  0% {
    background-position: -200px 0;
  }
  100% {
    background-position: 200px 0;
  }
}
```
